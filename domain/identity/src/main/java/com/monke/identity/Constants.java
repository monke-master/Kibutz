package com.monke.identity;

import java.util.UUID;

public class Constants {
    public static String MALE_ID = UUID.randomUUID().toString();
    public static String FEMALE_ID = UUID.randomUUID().toString();
}
